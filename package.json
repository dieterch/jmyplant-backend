{
  "private": true,
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev --host 0.0.0.0 --port 4567",
    "prepare": "nitro prepare",
    "preview": "node .output/server/index.mjs"
  },
  "devDependencies": {
    "nitropack": "latest",
    "prisma": "^6.1.0"
  },
  "dependencies": {
    "@prisma/client": "^6.1.0"
  }
}
